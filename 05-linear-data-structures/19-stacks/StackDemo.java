
import java.util.*;

public class StackDemo {

    public static void main(String[] args) {
        // demonstrate Deque<Integer> used as a stack (push/pop/peek)
        // call and print each method below
    }

    // return true if all brackets in s are balanced
    static boolean isBalanced(String s) {
        return false;
    }

    // evaluate a postfix expression like "2 3 + 4 *"; return integer result
    static int evalPostfix(String expr) {
        return 0;
    }

    // for each day return how many days until a warmer temperature (0 if none)
    static int[] daysToWarmer(int[] temps) {
        return new int[0];
    }

    // return area of the largest rectangle that fits in the histogram
    static int maxRectHistogram(int[] heights) {
        return 0;
    }

    // stack that supports push, pop, top, and getMin all in O(1)
    static class MinStack {
        // use two stacks: one for data, one to track minimums

        void push(int val) {

        }

        void pop() {

        }

        int top() {
            return 0;
        }

        int getMin() {
            return 0;
        }
    }
}
