
import java.util.*;

public class HashingDemo {

    public static void main(String[] args) {
        // demonstrate HashMap: put, get, containsKey, remove, getOrDefault, iterate entrySet
        // demonstrate HashSet: add, contains, no duplicates
        // call and print each method below
        // show LinkedHashMap (insertion order) and TreeMap (sorted order)
    }

    // return frequency map: each value → how many times it appears
    static Map<Integer, Integer> frequency(int[] arr) {
        return new HashMap<>();
    }

    // return indices [i, j] where arr[i] + arr[j] == target; else [-1,-1]
    static int[] twoSum(int[] arr, int target) {
        return new int[]{-1, -1};
    }

    // return true if any contiguous subarray sums to zero (prefix sum + set)
    static boolean hasZeroSumSubarray(int[] arr) {
        return false;
    }

    // return length of longest sequence of consecutive integers — O(n)
    static int longestConsecutive(int[] arr) {
        return 0;
    }

    // group words that are anagrams of each other; return list of groups
    static List<List<String>> groupAnagrams(String[] words) {
        return new ArrayList<>();
    }
}
