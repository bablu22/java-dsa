
import java.util.*;

public class RecursionDemo {

    public static void main(String[] args) {
        // call and print each method below
    }

    static long factorial(int n) {
        return 0;
    }

    // naive O(2^n) Fibonacci
    static int fib(int n) {
        return 0;
    }

    // sum of 1 to n
    static int sumN(int n) {
        return 0;
    }

    // fast power: base^exp in O(log n)
    static long power(long base, int exp) {
        return 0;
    }

    // Euclidean GCD
    static int gcd(int a, int b) {
        return 0;
    }

    // recursive binary search; return index or -1
    static int binarySearch(int[] a, int target, int lo, int hi) {
        return 0;
    }

    // print characters of s in reverse order using recursion
    static void printReverse(String s) {

    }

    // return true if s[l..r] is a palindrome
    static boolean isPalindrome(String s, int l, int r) {
        return false;
    }

    // print all subsets of a[idx..end], building current list in cur
    static void subsets(int[] a, int idx, List<Integer> cur) {

    }

    // Tower of Hanoi: move n disks from 'from' to 'to' using 'aux'
    static void hanoi(int n, char from, char to, char aux) {

    }
}
