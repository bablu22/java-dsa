
import java.util.*;

public class BacktrackingDemo {

    public static void main(String[] args) {
        // call and print output of each method below
    }

    // print all permutations of a[start..end]
    static void permutations(int[] a, int start) {

    }

    // print all subsets starting from index idx, building current list
    static void subsets(int[] a, int idx, List<Integer> cur) {

    }

    // print all combinations that sum to target (candidates may be reused)
    static void combinationSum(int[] candidates, int target) {

    }

    // helper for combinationSum
    static void combinationHelper(int[] c, int remaining, int start, List<Integer> cur) {

    }

    // solve N-Queens: place n queens so none attack each other; print all boards
    static void solveNQueens(int n) {

    }

    // place queens row by row on board
    static void nQueensHelper(char[][] board, int row, int n) {

    }

    // return true if placing a queen at (row, col) is safe
    static boolean isSafe(char[][] board, int row, int col, int n) {
        return false;
    }

    // fill empty cells (0) of the 9x9 board; return true if solved
    static boolean solveSudoku(int[][] board) {
        return false;
    }

    // return true if placing num at (row, col) is valid
    static boolean isValid(int[][] b, int row, int col, int num) {
        return false;
    }

    static void swap(int[] a, int i, int j) {

    }
}
