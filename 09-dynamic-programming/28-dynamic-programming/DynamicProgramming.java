
import java.util.*;

public class DynamicProgramming {

    public static void main(String[] args) {
        // call and print results of each method below
    }

    // fibonacci(n) with top-down memoisation
    static long fibonacci(int n) {
        return 0;
    }

    // minimum steps to reach nth stair (1 or 2 steps at a time) — bottom-up O(1) space
    static int climbStairs(int n) {
        return 0;
    }

    // minimum number of coins to make amount; return -1 if impossible
    static int coinChange(int[] coins, int amount) {
        return 0;
    }

    // maximum sum of non-adjacent houses
    static int houseRobber(int[] nums) {
        return 0;
    }

    // 0/1 knapsack: max value with given weight capacity
    static int knapsack(int[] weights, int[] values, int capacity) {
        return 0;
    }

    // length of longest common subsequence of strings a and b
    static int lcs(String a, String b) {
        return 0;
    }

    // length of longest strictly increasing subsequence — O(n log n)
    static int lis(int[] nums) {
        return 0;
    }

    // minimum edit distance (insert / delete / replace) to convert a into b
    static int editDistance(String a, String b) {
        return 0;
    }

    // number of unique paths in an m×n grid (only right or down moves)
    static int uniquePaths(int m, int n) {
        return 0;
    }

    // return true if nums can be split into two subsets with equal sum
    static boolean canPartition(int[] nums) {
        return false;
    }

    // length of longest palindromic subsequence of s
    static int longestPalinSubseq(String s) {
        return 0;
    }
}
