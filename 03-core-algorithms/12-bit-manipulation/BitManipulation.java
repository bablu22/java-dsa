
public class BitManipulation {

    public static void main(String[] args) {
        // call and print each method below
        // show multiply/divide by powers of 2 using shifts
        // check even/odd with &1
        // enumerate all subsets of size 3 using bitmask
    }

    // return the i-th bit of n (0 or 1)
    static int getBit(int n, int i) {
        return 0;
    }

    // return n with the i-th bit set to 1
    static int setBit(int n, int i) {
        return 0;
    }

    // return n with the i-th bit cleared to 0
    static int clearBit(int n, int i) {
        return 0;
    }

    // return n with the i-th bit flipped
    static int toggleBit(int n, int i) {
        return 0;
    }

    // return true if n is a power of 2
    static boolean isPowerOf2(int n) {
        return false;
    }

    // count number of set bits using Brian Kernighan's algorithm
    static int countSetBits(int n) {
        return 0;
    }

    // return position of lowest set bit
    static int lsbPosition(int n) {
        return 0;
    }

    // XOR all elements — the one without a pair remains
    static int findSingle(int[] arr) {
        return 0;
    }

    // print all 2^n subsets using bitmask iteration
    static void subsetsBitmask(int n) {

    }
}
