
import java.util.*;

public class ComplexityExamples {

    public static void main(String[] args) {
        // call each method and label its complexity in comments
    }

    // O(1) — return first element
    static int getFirst(int[] a) {
        return 0;
    }

    // O(log n) — iterative binary search; return index or -1
    static int binarySearch(int[] a, int target) {
        return 0;
    }

    // O(n) — return sum of all elements
    static long linearSum(int[] a) {
        return 0;
    }

    // O(n log n) — merge sort (sort in-place)
    static void mergeSort(int[] a, int l, int r) {

    }

    // O(n²) — print all pairs that sum to target
    static void quadraticPairs(int[] a, int target) {

    }

    // O(2^n) — print all subsets of a[idx..end]
    static void subsets(int[] a) {

    }

    // helper for subsets recursion
    static void subsetsHelper(int[] a, int idx, List<Integer> cur) {

    }

    // O(n!) — print all permutations
    static void permutations(int[] a, int start) {

    }

    static void swap(int[] a, int i, int j) {

    }
}
