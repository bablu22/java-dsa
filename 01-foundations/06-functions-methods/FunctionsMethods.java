
public class FunctionsMethods {

    public static void main(String[] args) {
        // call and print results of all methods below
        // demonstrate pass-by-value (primitive)
        // demonstrate pass-by-reference (array)
    }

    static int add(int a, int b) {
        return 0;
    }

    static long factorial(int n) {
        return 0;
    }

    static boolean isPrime(int n) {
        return false;
    }

    // Euclidean GCD
    static int gcd(int a, int b) {
        return 0;
    }

    // fast power using bit shifting: base^exp
    static long power(long base, int exp) {
        return 0;
    }

    // overloaded: max of two ints
    static int max(int a, int b) {
        return 0;
    }

    // overloaded: max of three ints
    static int max(int a, int b, int c) {
        return 0;
    }

    // varargs: sum of any number of ints
    static int sum(int... nums) {
        return 0;
    }

    // double every element in the array (modifies original)
    static void doubleArray(int[] arr) {

    }
}
