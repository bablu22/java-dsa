
public class ConditionalStatements {

    public static void main(String[] args) {
        // demonstrate if / else if / else
        // demonstrate switch-case with break
        // demonstrate ternary for simple condition
        // demonstrate short-circuit evaluation (null check)
    }

    // return grade letter based on score
    static String getGrade(int score) {
        return "";
    }

    // return "Even" or "Odd"
    static String parity(int n) {
        return "";
    }

    // classify number as Negative / Zero / Single digit / Multi digit
    static String classify(int n) {
        return "";
    }
}
