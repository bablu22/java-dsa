
public class TwoDArrays {

    public static void main(String[] args) {
        // create a 4x4 matrix and call each method below
    }

    // print matrix row by row
    static void print(int[][] m) {

    }

    // print all elements in row-major order
    static void rowMajor(int[][] m) {

    }

    // print elements in spiral (clockwise) order
    static void spiralOrder(int[][] m) {

    }

    // transpose matrix in-place (square matrix)
    static void transpose(int[][] m, int n) {

    }

    // rotate matrix 90 degrees clockwise in-place
    static void rotate90(int[][] m, int n) {

    }

    // search target in row-wise and col-wise sorted matrix; return true/false
    static boolean searchSorted(int[][] m, int target) {
        return false;
    }

    // return 2D prefix sum array
    static int[][] build2DPrefix(int[][] g) {
        return new int[0][0];
    }

    // return sum of submatrix from (r1,c1) to (r2,c2) using prefix array
    static int query2DPrefix(int[][] p, int r1, int c1, int r2, int c2) {
        return 0;
    }
}
