
public class StringsDemo {

    public static void main(String[] args) {
        // demonstrate common String methods (length, charAt, substring, etc.)
        // convert String to char array and back
        // use StringBuilder in a loop
        // call and print each method below
    }

    // return true if s is a palindrome
    static boolean isPalindrome(String s) {
        return false;
    }

    // return reversed string
    static String reverse(String s) {
        return "";
    }

    // return true if a and b are anagrams of each other
    static boolean isAnagram(String a, String b) {
        return false;
    }

    // return count of vowels in s
    static int countVowels(String s) {
        return 0;
    }

    // run-length encode: "aaabbc" → "a3b2c"
    static String compress(String s) {
        return "";
    }

    // sliding window: return length of longest substring without repeating characters
    static int longestNoRepeat(String s) {
        return 0;
    }
}
