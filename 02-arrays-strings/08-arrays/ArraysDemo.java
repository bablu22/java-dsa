
public class ArraysDemo {

    public static void main(String[] args) {
        // declare and initialise an array
        // call and print each method below
    }

    // return index of target, or -1 if not found
    static int linearSearch(int[] a, int target) {
        return 0;
    }

    // array must be sorted; return index of target or -1
    static int binarySearch(int[] a, int target) {
        return 0;
    }

    // return prefix sum array where prefix[i] = sum of a[0..i-1]
    static int[] buildPrefix(int[] a) {
        return new int[0];
    }

    // return sum of a[l..r] using prefix array
    static int rangeSum(int[] prefix, int l, int r) {
        return 0;
    }

    // return string of pair "(x, y)" from sorted array that sums to target, else "Not found"
    static String twoPointerPair(int[] sorted, int target) {
        return "";
    }

    // return maximum sum of any contiguous subarray of size k
    static int maxSumWindow(int[] a, int k) {
        return 0;
    }

    // Kadane's algorithm: return maximum subarray sum
    static int kadane(int[] a) {
        return 0;
    }

    // rotate array left by k positions (in-place)
    static void rotateLeft(int[] a, int k) {

    }

    // reverse a[l..r] in-place
    static void reverse(int[] a, int l, int r) {

    }
}
